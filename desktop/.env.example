# 接入配置模板 —— 复制为 .env.development / .env.production 后填入真实值
# 真实 env 文件已被 .gitignore 忽略，请勿提交密钥。

# ============ 自托管 HTTP Function 地址（主路径） ============
# 两个函数 auth-otp / data-api 部署在 Vercel，底层数据库为 Supabase PG。
# 见 docs/migrate-to-supabase.md 与 vercel/ 目录。
# 注意：base 必须带 /api/ 前缀，直接命中函数文件路由
#   （/api/auth-otp/[action]、/api/data-api/[...path]），
#   不走 vercel.json rewrites（Vercel 通配 rewrite 会丢 path 段，已弃用）。

# auth-otp HTTP function 根 URL（发码/验码/密码登录/注册/改密）
VITE_AUTH_OTP_URL=https://your-project.vercel.app/api/auth-otp

# data-api HTTP function 根 URL（业务 CRUD + 管理员统计）
VITE_DATA_API_URL=https://your-project.vercel.app/api/data-api
