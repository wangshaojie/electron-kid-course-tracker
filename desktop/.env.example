# 接入配置模板 —— 复制为 .env.development / .env.production 后填入真实值
# 真实 env 文件已被 .gitignore 忽略，请勿提交密钥。

# ============ 自托管 HTTP Function 地址（迁移后主路径） ============
# 两个函数 auth-otp / data-api 部署在 Vercel，底层数据库为 Supabase PG。
# 见 docs/migrate-to-supabase.md 与 vercel/ 目录。

# auth-otp HTTP function 根 URL（发码/验码/密码登录/注册/改密）
VITE_AUTH_OTP_URL=https://your-project.vercel.app/auth-otp

# data-api HTTP function 根 URL（业务 CRUD + 管理员统计）
VITE_DATA_API_URL=https://your-project.vercel.app/data-api

# ============ 旧 CloudBase 兜底（可选，迁移完成后删除） ============
# 仅当上面两个 URL 都未配置时才被用于拼默认地址，迁移完成后可整体移除。
# VITE_CLOUDBASE_ENV_ID=kid-course-tracker-d6c2816e966b5
