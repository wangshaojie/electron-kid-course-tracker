# ── 密钥 / 本地配置 ─────────────────────────────
# cloudbaserc.json 含 RESEND_API_KEY / TCB_SDK_SECRET / JWT_SECRET，禁止提交
cloudbaserc.json
cloudbaserc.local.json

# ── 日志 / 构建产物 ────────────────────────────
*.log
desktop/release/
desktop/dist/
desktop/dist-electron/
desktop/node_modules/
desktop/.vite/

# ── 调试 / 废弃残留 ────────────────────────────
.tmp/

# ── 工具链 / 编辑器 ────────────────────────────
.codebuddy/
skills-lock.json
.idea/
.vscode/
.DS_Store
Thumbs.db
